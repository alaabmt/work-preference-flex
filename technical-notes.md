# Technical notes — V17.2

Preference scoring logic is unchanged. Participant-facing numeric indices are intentionally hidden because the current scores are relative/ipsative pilot indices and should not be read as percentiles or standardized scores. Coach/research data retain the underlying scores. No empirically unsupported fixed threshold is used to declare co-leading constructs in the participant report.
